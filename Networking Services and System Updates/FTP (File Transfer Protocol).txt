FTP - FILE TRANSFER PROTOCOL

- become root
- rpm -qa | grep ftp 
- ping google.com
- yum install vsftpd
- vi /etc/vsftpd/vsftpd.confs

SCP --> file transfer encrypted from one server to another one

rsync -->it is mostly used to backup the files, directories from one server to another one. default port is 22 (same as SSH)